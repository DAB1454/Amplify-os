[project]
name = "amplify-observability"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "structlog>=24.0",
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
